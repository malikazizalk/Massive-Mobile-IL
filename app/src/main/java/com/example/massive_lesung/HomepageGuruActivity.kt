package com.example.massive_lesung

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class HomepageGuruActivity : AppCompatActivity()  {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_homepage_guru)

        //val btnProfile : Button = findViewById(R.id.profile_image)
       // btnProfile.setOnClickListener(this)
    }
   // override fun onClick(v: View) {
       // when (v.id) {
          //  R.id.profile_image -> {
           //     val bundle = Bundle()
            //    val intent = Intent(this@HomepageGuruActivity, Profile2::class.java)
           //     intent.putExtras(bundle)
           //     startActivity(intent)
          //  }
     //   }
   // }
}